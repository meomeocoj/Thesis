{
  "name": "@oraichain/blsdkg",
  "version": "1.0.3",
  "main": "pkg/blsdkg.js",
  "types": "pkg/blsdkg.d.ts",
  "private": true,
  "scripts": {
    "dev": "rimraf pkg/ && wasm-pack build --target web",
    "build": "wasm-pack build --target web --scope oraichain",
    "prepack": "wasm-pack pack"
  },
  "files": [
    "pkg",
    "src",
    "Cargo.lock",
    "Cargo.toml"
  ],
  "devDependencies": {
    "wasm-pack": "^0.12.1"
  },
  "license": "MIT",
  "engines": {
    "node": ">=14.17.0",
    "npm": ">=6.x"
  }
}
